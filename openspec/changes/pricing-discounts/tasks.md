## 1. Implementation

- [x] 1.1 Implement discount calculation logic in `app/src/discounts.ts` including tier logic, coupon sequential logic, and rounding. Verify by running `npm test`. (Verified: passes)
- [x] 1.2 Export `priceOrder` function. Verify by ensuring no type errors. (Verified)

## 2. Testing

- [x] 2.1 Write tests mapping to each AC in `app/src/discounts.test.ts`. Verify by running `npm test` and getting 100% pass on these AC tests. (Verified: passes)

## 3. Documentation

- [x] 3.1 Update `docs/traceability.md`. Verify it exists and is filled. (Verified)
- [x] 3.2 Update `docs/sdd-tool.md` and `docs/ab-validation.md`. (Verified)
